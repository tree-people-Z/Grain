# ---------------------------------------------------------------------------
# Grain dependencies
#
# Base functionality (import a subtitle, review, export SRT/ASS, the manual
# engine) has ZERO third-party dependencies — just Python 3.10+ and ffmpeg.
# Everything below is only needed for the pyannote auto-detection engine.
#
#     pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- pyannote speaker detection --------------------------------------------
torch>=2.6,<3
torchaudio>=2.6,<3
pyannote.audio>=3.1,<5          # community-1 (4.x) preferred, 3.1 fallback

# Optional NumPy acceleration for waveform display (also pulled in by torch).
numpy>=1.24,<3
