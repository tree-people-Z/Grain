# ---------------------------------------------------------------------------
# Grain dependency sets
#
# Base functionality (import / review / export, built-in + manual engines) has
# ZERO third-party dependencies — just Python 3.10+ and ffmpeg on PATH. Nothing
# below is needed unless you want an automatic detection engine.
#
# Install everything for the professional engines (recommended):
#     pip install -r requirements.txt
#
# Upper bounds are deliberate: torch/pyannote/datasets ship breaking changes on
# new majors, and an unbounded `pip install` used to pull them silently.
# ---------------------------------------------------------------------------

# --- Detection engines -----------------------------------------------------
torch>=2.6,<3
torchaudio>=2.6,<3
pyannote.audio>=3.1,<5          # community-1 (4.x) preferred, 3.1 fallback
funasr>=1.1,<2                  # 3D-Speaker / CAM++ engine
modelscope>=1.20,<2             # model download for CAM++
addict>=2.4
simplejson>=3.19
datasets>=2.20,<6
hdbscan>=0.8.40

# Optional NumPy acceleration for the built-in feature path (also pulled in by
# torch). Everything still works without it.
numpy>=1.24,<3

# --- Optional: ASR transcript when a file has no subtitles -----------------
# faster-whisper

# --- Optional: desktop shell (启动桌面版.bat / desktop_pywebview.py) ------
# pywebview
# pyinstaller

# --- Optional: external engines (Sortformer) ---------------------------------
# Their isolated venv is created with uv; see scripts/setup_engine.py
# uv
